import React from 'react';
import { TouchableOpacity } from 'react-native-gesture-handler';
import Login from '../dbscreens/LogScreen';

const LoginScreen = ( { navigation } ) => {
    return (
      <Login/>
    );
  }


  export default LoginScreen;
