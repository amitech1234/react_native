import React from 'react';
import { Text, View, TouchableOpacity, StyleSheet, TextInput} from 'react-native';
import Search from '../dbscreens/SearchDb'
const SearchScreen = ( { navigation } ) => {
    return (
      <Search/>
    );
  }
  export default SearchScreen;


